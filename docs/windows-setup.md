# Windows Setup

Required tools:

- Rust stable with the MSVC target
- Visual C++ Build Tools and a Windows SDK
- `cloudflared`
- PowerShell on Windows 10 or 11

After installation, open a new PowerShell window so updated `PATH` entries are loaded. Verify with `cargo --version` and `cloudflared --version` before starting the PoC.
