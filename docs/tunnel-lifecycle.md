# Tunnel Lifecycle

Each Quick Tunnel session can receive a different `trycloudflare.com` hostname.

1. Start the PoC and copy the printed HTTPS URL.
2. Install that exact origin on the iPhone Home Screen.
3. Complete push testing while the tunnel stays running.
4. Stop both processes when finished.

If the hostname changes, remove the old Home Screen app and install the new origin.
